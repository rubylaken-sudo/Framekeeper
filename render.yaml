services:
  - type: web
    name: framekeeper-site
    runtime: static
    staticPublishPath: streamer-site
    pullRequestPreviewsEnabled: false
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
